spring:
  application:
    name: spring-jwt
  datasource:
    url: ${DB_URL:jdbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:5432/postgres?sslmode=require}
    username: ${DB_USER:postgres.bcmejmpuchqvchvhsjcc}
    password: ${DB_PASSWORD:D8T2nmePBcua}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        '[format_sql]': true
        '[use_sql_comments]': true
server:
  port: ${PORT:8080}